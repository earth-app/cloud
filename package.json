{
  "private": true,
  "name": "@earth-app/cloud",
  "version": "1.0.0",
  "description": "The Automaton behind The Earth App",
  "scripts": {
    "dev": "bunx wrangler dev --port 9898",
    "deploy": "bunx wrangler deploy --minify",
    "prettier": "bunx prettier --write .",
    "prettier:check": "bunx prettier --check .",
    "prepare": "husky install"
  },
  "dependencies": {
    "@cloudflare/workers-types": "^4.20251014.0",
    "@earth-app/ocean": "1.0.0-a752a2e",
    "@types/pako": "^2.0.4",
    "hono": "^4.10.1",
    "pako": "^2.1.0"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.2.5",
    "prettier": "^3.6.2",
    "wrangler": "^4.43.0"
  },
  "lint-staged": {
    "*.{js,ts,json,yml,md}": "prettier --write"
  }
}
